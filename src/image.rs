/*extern crate viuer;
#[warn(dead_code)]
pub fn display_image()
{
    let image_conf = viuer::Config {
        transparent: false,
        absolute_offset: false,
        x: 5,
        y: 5,
        restore_cursor: false,
        width: Some(200),
        height: Some(200),
        truecolor: false,
        use_kitty: false,
        use_iterm: false,
    };

    // fixme
    viuer::print_from_file("smol_makima.png", &image_conf).expect("Failed to display image");
}*/